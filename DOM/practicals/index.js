/* 
Accessing Elements: Methods like getElementById() (selects by id),
                                    getElementsByClassName() (selects by class),
                                    getElementsByTagName() (selects by tag name), 
                                    and querySelector()/querySelectorAll() 
                                    (selects using CSS selectors).


Manipulating Content: 
Properties such as textContent (gets or sets text), 
innerHTML (gets or sets HTML content), and style (sets CSS styles).


Modifying Structure: Methods like createElement() (creates new elements), 
appendChild() (adds child nodes), and remove() (removes elements).


Event Handling: 
The addEventListener() method attaches event handlers (like "click") to elements. */



//document.getElementById("head").innerHTML ="lets goo!";
//document.getElementById("head").style.color="red";
